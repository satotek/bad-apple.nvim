[package]
name = "bav-engine"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bav-format = { path = "../bav-format" }
rodio = { version = "0.22.2", default-features = false, features = ["playback", "mp3"] }
